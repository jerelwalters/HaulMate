# GitHub automation

Workflow files belong in `workflows/`. The initial CI workflow will be added
with `P0-REL-01` after its clean-checkout iOS and Supabase commands are verified.

Keep pilot deployments manual until the corresponding environment protection,
secrets, and rollback steps are documented.
